apiVersion: v1
kind: Service
metadata:
  name: cms0
  labels:
    app.kubernetes.io/name: cms0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cms0
  ports:
    - name: http
      port: 80
      targetPort: http
